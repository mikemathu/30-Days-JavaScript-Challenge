// Array constructor
const arr = Array()

// or

let arr = new Array()

// Using sqare brackets([]) the most recommended way to create an empty list

const arr = []

// Creating an array with values
const numbers = [0, 3.14, 9.18, 37, 98.6, 100]


// Array with different data types
const arr = [
    'Mike',
    250,
    true,
    {country: 'Kenya', city: 'Nairobi'},
    {skills: ['HTML', 'CSS', 'JS', 'React', 'PHP', 'Laravel']}
]